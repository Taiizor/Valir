{
    "_securityWarning": "⚠️ SECURITY WARNING: This configuration file contains DEMO credentials for local development only. DO NOT use these credentials in production environments. Always use strong passwords, secrets management, and environment-specific configuration.",
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },
    "ConnectionStrings": {
        "_comment": "⚠️ DEMO CREDENTIALS - Change for production use",
        "Postgres": "Host=localhost;Port=5432;Database=valir;Username=valir;Password=valir"
    },
    "Redis": {
        "_comment": "⚠️ DEMO CONFIGURATION - Secure your Redis instance in production",
        "ConnectionString": "localhost:6379"
    },
    "RabbitMQ": {
        "_comment": "⚠️ DEMO CREDENTIALS - Change for production use",
        "HostName": "localhost",
        "Port": 5672,
        "UserName": "guest",
        "Password": "guest"
    },
    "Kafka": {
        "_comment": "⚠️ DEMO CONFIGURATION - Secure your Kafka cluster in production",
        "BootstrapServers": "localhost:9092"
    }
}
